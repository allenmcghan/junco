# Tools

Nothing here yet.

The first tool to write is the log recovery utility, which scans raw sectors for the record magic header and reconstructs a flight from a card whose filesystem is gone. It is part of the log format specification being credible, not an accessory to it.

See spec/log-format.md.
